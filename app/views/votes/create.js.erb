$('#vote_count_<%= @link.id %>').html('<strong><%=@link.votes.count %></strong>');
$('#upvote_<%= @link.id %>').html('');

<% if false %>
// $('#upvote_<%=link.id %>').html('<%= link_to "-", vote_path(:id => current_user.id, :vote => {:link_id => @link.id, :user_id => @current_user.id}), :method => :delete, remote: true %>');
// $.ajax({
// 	type: "POST",
// 	url: 
// }) 
<% end %>