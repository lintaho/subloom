<div id="follow_form">
	<% if !current_user %>
		<%= render 'users/follow' %>
	<% elsif current_user != @user %>
		<% if !current_user.following?(@user) %>
			<%= render 'users/follow' %>
		<% else %>
			<%= render 'users/unfollow' %>
			<% end %>
	<% end %>	
</div>

