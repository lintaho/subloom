<%= form_tag({:controller => :follow_relationships, :action => :create}, remote:true) do |f| %>
	<%= hidden_field_tag :followed_id, @user.id %>
	<%= submit_tag "Follow", class:"btn btn-large btn-primary" %>
<% end %>