
<%= form_for(current_user.follow_relationships.find_by(followed_id: @user.id), html: {method: :delete}, remote: true) do |f| %>

	<%= f.hidden_field :followed_id %>
	<%= f.submit "Unfollow", class:"btn btn-large" %>
<% end %>